# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build techradar-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t techradar-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system techradar-api && \
          adduser --system -G techradar-api techradar-api

COPY  dist/apps/techradar-api techradar-api
COPY  node_modules techradar-api/node_modules
RUN chown -R techradar-api:techradar-api .

USER techradar-api

CMD ["node", "techradar-api"]
